<!DOCTYPE html>
<html>
  <head>
    <title>RailsLongestWordGame</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <h1 class="fw-light">New game</h1>
    <%= @letters.join(' ') %>
    <p class="mt-3 fw-light">What is the longest word you can find?</p>
    <form action="/score" method="post" data-turbo="false">
      <div class="input-group mb-3">
        <input class="form-control" type="text" name="word" value="<%= params[:word] %>">
        <input class="btn btn-outline-secondary" type="submit" id="button-addon2" value="Play">
</div>
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    </form>
  </body>
</html>
